Rendered from test fixtures by `make sample`. Nothing in it is real: the
account, principals, alert and run are canned data from `internal/azure/azuretest`.
Open the `.html` file in a browser to see what a record looks like.
